[project]
name = "scalable-ai-assistants"
version = "0.1.0"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "dspy>=3.1.3",
]

[dependency-groups]
dev = [
    "mypy>=1.19.1",
    "ruff>=0.15.0",
]

[tool.uv.workspace]
members = [
    "dspy",
]
